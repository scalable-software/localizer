{
  "Component": "Localizer",
  "Package": "localizer",
  "Description": "The Localizer is a utility class that stores localized bundles, resolves the active localization from the current language, listens to global application configuration changes, and emits an event when the language changes. It is an EventTarget-based utility rather than a custom element: it declares no Composition or Tag, and its single gesture is named after the window event it listens to (`onappconfigchange`) rather than after a user interaction.",
  "Data": {
    "localizations": {
      "type": "Localizations<T>",
      "description": "A record of language codes mapped to localization bundles. Keys are language codes; values are localization bundles or undefined. Supplied once through the constructor and internally owned thereafter.",
      "readonly": true
    },
    "options": {
      "type": "Options",
      "description": "Constructor options, supplied once and internally owned. A bare string passed as the second constructor argument is treated as `{ language }`. `language` is an explicit initial language and wins over storage. `storage` names a localStorage key holding the persisted language; it is read once at construction and never written, and when absent storage is not consulted.",
      "properties": {
        "language": {
          "type": "string",
          "description": "Explicit initial language; normalized. Wins over the persisted and browser languages."
        },
        "storage": {
          "type": "string",
          "description": "localStorage key holding the persisted language, read once at construction and never written. No default — when absent, storage is not consulted. Absent, empty, or unreadable values fall through to the browser language."
        }
      },
      "default": {},
      "readonly": true
    }
  },
  "States": {
    "language": {
      "type": "Language",
      "description": "The active language used to resolve the current lexicon. The constructor initializes this value from the normalized browser language or from the normalized constructor argument (`en-US` becomes `en`). Later assignments through `language` or `setLanguage()` are stored verbatim. Falsy assignments are ignored; non-string assignments throw `Invalid language value: <value>`. Setting the same value does not emit an event.",
      "values": ["<language code>"],
      "default": "<navigator.language normalized to base lowercase language code>",
      "readonly": false
    },
    "lexicon": {
      "type": "T",
      "description": "The resolved localization bundle for the current language. Resolution order: the bundle matching `language`, then the English bundle `en`, then the first available bundle. Throws `Localizer: no localization bundles are available.` when none exists.",
      "values": ["<localization bundle>"],
      "default": "<resolved from localizations>",
      "readonly": true
    }
  },
  "Operations": {
    "initialize": {
      "description": "Registers the Localizer to listen for global application configuration changes on `window`. Idempotent: calling it while already initialized has no effect.",
      "parameters": [],
      "returns": "void"
    },
    "dispose": {
      "description": "Removes the global application configuration listener previously registered by `initialize()`. Idempotent: calling it while not initialized has no effect.",
      "parameters": [],
      "returns": "void"
    },
    "setLanguage": {
      "description": "Updates the active language by delegating to the `language` setter.",
      "parameters": [
        {
          "name": "language",
          "type": "string",
          "description": "The new language value to store. Falsy values are ignored."
        }
      ],
      "returns": "void"
    }
  },
  "Events": {
    "onlanguagechange": {
      "description": "Triggered when the active language changes to a new truthy value. Not triggered when the assigned language is falsy or equals the current language.",
      "parameters": [
        {
          "name": "detail",
          "type": "object",
          "description": "Contains the new active language.",
          "properties": {
            "language": {
              "type": "string"
            }
          }
        }
      ]
    }
  },
  "Gestures": {
    "onappconfigchange": {
      "description": "A window-level custom event used to synchronize the Localizer with application configuration changes. Observed only after `initialize()` has been called and until `dispose()`. The event `detail` may carry an optional `language` string.",
      "triggers": {
        "operation": "setLanguage",
        "condition": "on window, after initialize(), when detail.language is present"
      }
    }
  }
}
